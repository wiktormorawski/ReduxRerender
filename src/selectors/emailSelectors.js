// import { useSelector } from "react-redux";

// export const emailsUnread = (emails) =>
//   emails.filter((email) => email.is_unread);
// export const getEmailById = (emails, id) =>
//   emails.filter((email) => email.id === parseInt(id))[0];

// export const getEmailByIdSelector = (id) =>
//   useSelector(
//     (state) => state.emails.filter((email) => email.id === parseInt(id))[0]
//   );
// export const emailsUnreadSelector = useSelector((state) =>
//   state.emails.filter((email) => email.is_unread)
// );
